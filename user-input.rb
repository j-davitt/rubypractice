# run the application using `ruby filename.rb` in terminal

puts "Enter your name: "
name = gets.chomp() #when name is entered it registers the enter key, this removes the new line
puts ("Hello #{name}!")